# ============================================================================
# Farmer's Choice Market — CI
#
# Job "checks" — lint, typecheck and unit tests. No secrets needed; runs on
#                every push and pull request.
# Job "smoke"  — the end-to-end smoke test against a real Supabase project.
#                Skips automatically when secrets are absent (forks), so PRs
#                stay green.
#
# Required repository secrets (Settings → Secrets and variables → Actions):
#   NEXT_PUBLIC_SUPABASE_URL
#   NEXT_PUBLIC_SUPABASE_ANON_KEY
#   SUPABASE_SERVICE_ROLE_KEY
#   SMOKE_ADMIN_EMAIL   (optional — an admin account the test may reuse;
#                        a per-run admin is created and removed otherwise)
# ============================================================================

name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Lint, typecheck & unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint (must be error-free)
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit tests
        run: npm test

  smoke:
    # TEMPORARY probe: booleans embedded in the job display name so secret
    # visibility can be verified via the API. Revert once configured.
    name: "PROBE url=${{ secrets.NEXT_PUBLIC_SUPABASE_URL != '' }} anon=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY != '' }} svc=${{ secrets.SUPABASE_SERVICE_ROLE_KEY != '' }} fc=${{ secrets.FC_SEC != '' }} var=${{ vars.NEXT_PUBLIC_SUPABASE_URL != '' }}"
    needs: checks
    runs-on: ubuntu-latest
    # Runs only on pushes to main. The `secrets` context is not available in
    # step/job `if` conditions at all, so availability is detected via a
    # job-level env var instead (env may reference secrets).
    if: ${{ github.event_name == 'push' }}
    env:
      HAVE_SUPABASE_SECRETS: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL != '' }}
      # Temporary diagnostics — presence booleans only, never values.
      DIAG_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL != '' }}
      DIAG_ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY != '' }}
      DIAG_SVC: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY != '' }}
      DIAG_FC_SEC: ${{ secrets.FC_SEC != '' }}
      DIAG_VAR_URL: ${{ vars.NEXT_PUBLIC_SUPABASE_URL != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Diagnose secret visibility (booleans only)
        run: |
          echo "secrets.NEXT_PUBLIC_SUPABASE_URL visible:      $DIAG_URL"
          echo "secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY visible:  $DIAG_ANON"
          echo "secrets.SUPABASE_SERVICE_ROLE_KEY visible:      $DIAG_SVC"
          echo "secrets.FC_SEC visible:                         $DIAG_FC_SEC"
          echo "vars.NEXT_PUBLIC_SUPABASE_URL (Variables tab):  $DIAG_VAR_URL"

      - name: Start dev server
        if: ${{ env.HAVE_SUPABASE_SECRETS == 'true' }}
        run: |
          # The smoke test waits for /api/health itself; give it a head start.
          npm run dev &
          sleep 5

      - name: Run smoke test
        if: ${{ env.HAVE_SUPABASE_SECRETS == 'true' }}
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SMOKE_ADMIN_EMAIL: ${{ secrets.SMOKE_ADMIN_EMAIL }}
          APP_URL: http://localhost:3000
        run: npm run smoke

      - name: Report skipped smoke run
        if: ${{ env.HAVE_SUPABASE_SECRETS != 'true' }}
        run: echo "Supabase secrets not configured — smoke test skipped (unit checks still ran)."
